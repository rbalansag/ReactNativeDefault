import { tEmail, tKey, tPassword, tValue } from "../types";

// form
export const email: tEmail = "";
export const password: tPassword = "";

export const initialKey: tKey = "";
export const initialValue: tValue = "";